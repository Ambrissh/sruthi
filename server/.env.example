# Public URL where the React frontend is deployed. Leave unset only when a
# reverse proxy serves both the frontend and API from the same origin.
CLIENT_ORIGIN=https://www.shruthiswaralaya.com

# Defaults to 8787 in local development.
PORT=8787
